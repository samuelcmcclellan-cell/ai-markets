# Claude Code Prompt: Swap Reasoning and Agentic AI positions on the timeline

In `components/slides/03_WhatIsAI.tsx`, find the `timeline` array and make two changes:

1. Change the **Reasoning** entry's `position` from `"below"` to `"above"`
2. Change the **Agentic AI** entry's `position` from `"above"` to `"below"`

No other changes needed. Run `npm run build` to verify.
